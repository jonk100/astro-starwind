---
/**
 * ContactSection
 * Contact information + optional form slot.
 */

import { Section, ContentWidth, Stack, Cluster } from '@/components/layout';
import { Image } from '@/components/starwind/image';


interface Props {
  title?: string;
  description?: string;
  imgSrc?: string;
}

const {
  title = 'Contact',
  description,
  imgSrc,
} = Astro.props;
---

<Section size="lg">
  <Cluster>
    <Image src={imgSrc} inferSize />
    <Stack>
      {title && <h2>{title}</h2>}
      {description && <p>{description}</p>}
      
    </Stack>
  <ContentWidth width="md">
    <Stack gap="lg">

      <Stack gap="sm">
        <h2>{title}</h2>
        {description && <p>{description}</p>}
      </Stack>

      <slot />

    </Stack>
  </ContentWidth>
</Section>