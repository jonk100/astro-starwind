---
import { Stack, Cluster } from "@/components/layout";
import { Input } from "@/components/starwind/input";
import { Label } from "@/components/starwind/label";
import { Textarea } from "@/components/starwind/textarea";
import { Button } from "@/components/starwind/button";
---

<section
  class="
		flex
		flex-col
		bg-background
		py-16
    border-y
    border-border
  "
>
  <Stack
    class="
      items-center
			m-auto
			max-w-4xl
			w-full
			px-4
    "
    gap="lg"
  >
    <h2
      class="
        mb-4
        text-4xl
        font-serif
        font-semibold
       	leading-tight
        text-foreground
        text-center
      "
    >
      Send Us a Message
    </h2>

    <p
      class="
        mx-auto
        mb-8
        max-w-[37.5rem]
        text-base
        leading-relaxed
        text-muted-foreground
        text-center
        font-sans
      "
    >
      Fill out the form below and we'll get back to you within 24 hours to support your journey.
    </p>

    <form class="w-full max-w-2xl mx-auto bg-card border border-border/80 rounded-2xl p-8 shadow-sm font-sans">
      <Stack gap="lg">
        <Cluster class="flex-wrap gap-4">
          <div class="flex-1 min-w-[200px] space-y-2">
            <Label for="name" size="lg" class="text-foreground">Name</Label>
            <Input
              id="name"
              name="name"
              placeholder="Your name"
              size="lg"
              required
            />
          </div>
          <div class="flex-1 min-w-[200px] space-y-2">
            <Label for="email" size="lg" class="text-foreground">Email</Label>
            <Input
              id="email"
              name="email"
              type="email"
              placeholder="your@email.com"
              size="lg"
              required
            />
          </div>
        </Cluster>

        <div class="space-y-2">
          <Label for="subject" size="lg" class="text-foreground">Subject</Label>
          <Input
            id="subject"
            name="subject"
            placeholder="How can we help?"
            size="lg"
            required
          />
        </div>

        <div class="space-y-2">
          <Label for="message" size="lg" class="text-foreground">Message</Label>
          <Textarea
            id="message"
            name="message"
            placeholder="Tell us more about your inquiry..."
            size="lg"
            rows="6"
            required
            class="min-h-[150px]"
          />
        </div>

        <Button
          type="submit"
          size="lg"
          variant="primary"
          class="w-full py-4 text-lg"
        >
          Send Message
        </Button>
      </Stack>
    </form>
  </Stack>
</section>
